1935. Maximum Number of Words You Can Type


class Solution {
    public int canBeTypedWords(String text, String brokenLetters) {
        // Step 1: Mark broken letters
        boolean[] broken = new boolean[26];
        for (char ch : brokenLetters.toCharArray()) {
            broken[ch - 'a'] = true;
        }

        int result = 0;
        boolean canType = true; // assume we can type the word

        // Step 2: Scan through each character
        for (int i = 0; i < text.length(); i++) {
            char ch = text.charAt(i);

            if (ch == ' ') { // End of a word
                if (canType) {
                    result++;
                }
                canType = true; // reset for next word
            } else if (broken[ch - 'a']) {
                canType = false; // this word has a broken letter
            }
        }

        // Step 3: Handle the last word
        if (canType) {
            result++;
        }

        return result;
    }
}
